import {connect} from 'react-redux';
import MainDesc from '../components/MainDesc';

const mapStateToProps = (state) => {
  return {
  };
};

const mapDispatchToProps = (dispatch) => {
  return {
  };
};

const MainDescContainer = connect(
  mapStateToProps,
  mapDispatchToProps
)(MainDesc);

export default MainDescContainer;
